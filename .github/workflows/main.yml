name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run unittest
        run: python -m unittest discover -t . -s test -v

      # TODO: setup offscreen render on server (cf. https://doc.qt.io/qt-5/qtest-overview.html)
      # - name: Run offscreen render
      #   run: python batch.py --out-dir shaders/images --format png shaders/example*.glsl
